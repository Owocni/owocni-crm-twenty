# Owocni Ops (Stan systemu)

Faza 2: sidebar → `STANDALONE_PAGE` + front component. Semafory z GCS `ui.json` (fetch), fallback GET `/s/health/snapshot`. Zero live probe.

UUIDy muszą być v4. Nowe encje: `yarn twenty dev:add`.
