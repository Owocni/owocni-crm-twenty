{
  "name": "system-health-check",
  "version": "1.0.0",
  "description": "Owocni CRM health probe + email (SYSTEM_HEALTH Faza A). no_emit.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test evaluate.test.js",
    "start": "functions-framework --target=processSystemHealthCheck --port=8080"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "nodemailer": "^6.9.16"
  }
}
